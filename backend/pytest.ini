[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
addopts = --strict-markers
markers =
    unit: fast isolated business-logic tests
    integration: FastAPI endpoint tests with dependency overrides
    e2e: end-to-end tests
